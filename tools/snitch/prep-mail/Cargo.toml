[package]
name = "prep-mail"
version = "0.1.0"
authors = ["Azure IoT Edge Devs"]
publish = false

[dependencies]
env_logger = "0.5"
handlebars = "1.0"
serde = "1.0"
serde_json = "1.0"

snitcher = { path = "../snitcher" }