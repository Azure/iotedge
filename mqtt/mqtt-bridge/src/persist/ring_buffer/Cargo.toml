[package]
name = "ring_buffer"
version = "0.1.0"
authors = ["Azure IoT Edge Devs"]
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive", "rc"] }
bincode = "1.3.1"
getset = "0.1.1"
memmap = "0.7.0"

[dev-dependencies]
criterion = "0.3"
rand = "0.8.0"

[[bench]]
name = "ring_buffer_bench"
harness = false
