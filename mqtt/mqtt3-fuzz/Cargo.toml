[package]
name = "mqtt3-fuzz"
version = "0.1.0"
authors = ["Azure IoT Edge Devs"]
license = "MIT"
edition = "2018"
publish = false

[dependencies]
afl = "0.12"
mqtt3 = { path = "../mqtt3" }
bytes = "1"
tokio-util = { version = "0.7", features = ["codec"] }

[build-dependencies]
bytes = "1"
mqtt3 = { path = "../mqtt3" }
tokio-util = { version = "0.7", features = ["codec"] }
