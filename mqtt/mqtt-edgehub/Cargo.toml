[package]
name = "mqtt-edgehub"
version = "0.1.0"
authors = ["Azure IoT Edge Devs"]
edition = "2018"

[dependencies]
lazy_static = "1.4"
regex = "1"
tracing = "0.1"

mqtt3 = { path = "../mqtt3", features = ["serde1"] }
