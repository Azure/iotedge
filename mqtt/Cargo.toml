[workspace]
members = [
    "edgelet-client",
    "mqtt3",
    "mqtt-broker",
    "mqtt-broker-tests-util",
    "mqtt-edgehub",
    "mqtt-generic",
    "mqtt-policy",
    "mqtt-util",
    "mqttd",
    "policy",
    "mqtt-bridge/src/persist/ring_buffer"
]

[profile.release]
debug = true
