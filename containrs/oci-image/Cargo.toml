[package]
name = "oci-image"
version = "0.1.0"
authors = ["Azure IoT Edge Devs"]
edition = "2018"

[dependencies]
oci-common = { path = "../oci-common" }
oci-digest = { path = "../oci-digest" }
oci-runtime = { path = "../oci-runtime" }

serde = { version = "1.0", features = ["derive"] }
