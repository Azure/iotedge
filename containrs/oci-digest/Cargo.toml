[package]
name = "oci-digest"
version = "0.1.0"
authors = ["Azure IoT Edge Devs"]
edition = "2018"

[features]
default = ["serde"]

[dependencies]
hex = "0.4.0"
serde = { version = "1.0", optional = true }
sha2 = "0.8.0"
