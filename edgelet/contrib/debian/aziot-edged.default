# Defaults for aziot-edged initscript
# sourced by /etc/init.d/aziot-edged
# installed at /etc/default/aziot-edged by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_ARGS="-c /etc/aziot/edged/config.yaml"
