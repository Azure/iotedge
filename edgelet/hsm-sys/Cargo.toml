[package]
name = "hsm-sys"
version = "0.1.0"
authors = ["Azure IoT Edge Devs"]
links = "iothsm"
build = "build.rs"

[dependencies]

[build-dependencies]
cmake = "0.1"

[dev-dependencies]
num_cpus = "1.0"
