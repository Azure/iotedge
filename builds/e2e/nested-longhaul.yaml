trigger: none
pr: none
schedules:
- cron: "0 23 * * 4"
  displayName: Weekly run Thursday night
  branches:
    include:
    - release/1.2
  always: true

variables:
  NugetSecurityAnalysisWarningLevel: warn
  l3IpAddressMqtt: '' # TODO: see if we can remove

resources:
  pipelines:
  - pipeline: images
    source: 'Azure-IoT-Edge-Core Build Images'
    branch: 'release/1.2'
  - pipeline: packages
    source: 'Azure-IoT-Edge-Core Edgelet Packages'
    branch: 'release/1.2'

stages:
- stage: LockAgents 
  jobs:
  - template: templates/lock-test-agents.yaml
    parameters:
      upstream.protocol: 'amqp'
  - template: templates/lock-test-agents.yaml
    parameters:
      upstream.protocol: 'mqtt'

  # Get the ip address of the L3 agent. Needed for test modules on the middle level L4 agent.
  - job: Get_L3_IP_mqtt
    dependsOn:
      - Lock_Nested_Agents_mqtt
    displayName: Get ip address of L3 mqtt agent
    timeoutInMinutes: 180
    pool:
     name: $(pool.name)
     demands:
       - agent-group -equals $(agent.group)
       - Agent.OS -equals Linux
       - Agent.OSArchitecture -equals X64
       - status -equals unlocked_$(Build.BuildId)_L3_mqtt
    steps:
      - script: |
          L3_IP_ADDRESS_MQTT=$(ip route get 8.8.8.8 | sed -n '/src/{s/.*src *\([^ ]*\).*/\1/p;q}')
          echo "Ip adress of L3 agent: $L3_IP_ADDRESS_MQTT"
          echo "##vso[task.setvariable variable=l3IpAddressMqtt;isOutput=true]${L3_IP_ADDRESS_MQTT}"
        displayName: Get ip address of L3 mqtt agent
        name: get_l3_ip_mqtt
      - script: |
          echo $(l3IpAddressMqtt)

- stage: SetupVMs
  dependsOn: LockAgents
  variables: 
    l3IpAddressMqtt: $[ stageDependencies.LockAgents.Get_L3_IP_mqtt.outputs['get_l3_ip_mqtt.l3IpAddressMqtt'] ]
  jobs:
  - template: templates/nested-parent-vm-setup.yaml
    parameters:
      upstream.protocol: amqp
      test.l4DeploymentFileName: 'nestededge_middleLayerBaseDeployment_amqp.json'
  - template: templates/nested-longhaul-deploy-amd64.yaml
    parameters:
      upstream.protocol: amqp
      testInfo.testName: 'longhaul (nested-non-broker)'
  - template: templates/nested-parent-vm-setup.yaml
    parameters:
      upstream.protocol: mqtt
      test.l4DeploymentFileName: 'nestededge_middleLayer_messaging_mqtt.json'
      l3IpAddress: '$(l3IpAddressMqtt)'
  - template: templates/nested-longhaul-deploy-amd64.yaml
    parameters:
      upstream.protocol: mqtt
      testInfo.testName: 'longhaul (nested-broker)'

  # TODO: If i don't revert this commit pls make sure this works in all cases
  - job:  Unlock_agents      
    dependsOn:
      - SetupVM_level5_amqp
      - SetupVM_level5_mqtt
      - SetupVM_level4_amqp
      - SetupVM_level4_mqtt
      - Deploy_Longhaul_Linux_Amd64_amqp
      - Deploy_Longhaul_Linux_Amd64_mqtt
    condition: and(or(eq(dependencies.Lock_Nested_Agents_amqp.result, 'succeeded'), eq(dependencies.Lock_Nested_Agents_mqtt.result, 'succeeded')), eq(variables['test.pipeline'], 'true'))
    displayName: Unlock agents
    pool:
      name: $(pool.name)
      demands:
        - agent-group -equals $(agent.group)
        - Agent.OS -equals Linux
        - Agent.OSArchitecture -equals X64
        - status -equals unlocked_$(Build.BuildId)_L3_mqtt
    steps:
      - template: templates/nested-get-secrets.yaml     
      - template: templates/unlock-test-agents.yaml 